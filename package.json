{
  "name": "markipedia",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "get-notes": "git submodule update --init --recursive",
    "update-notes": "npm run get-notes git submodule foreach pull",
    "start": "npm run update-notes; npx @11ty/eleventy --serve",
    "build": "npm run update-notes; npx @11ty/eleventy",
    "debug": "DEBUG=Eleventy* npm run build",
    "start-debug": "DEBUG=Eleventy* npm run start",
    "benchmark": "DEBUG=Eleventy:Benchmark* npx @11ty/eleventy"
  },
  "keywords": [],
  "author": "",
  "license": "AGPLv3",
  "devDependencies": {
    "@11ty/eleventy": "^1.0.2"
  }
}
